---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-rbd-pvc
  namespace: cloudy
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: rook-ceph-block
